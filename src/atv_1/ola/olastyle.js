import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    name: {
        fontSize: 25,
        color: 'grey',
        marginTop: 20,
    
    },

});

export default styles;
